diff --git a/Classes/Middleware/McpServerMiddleware.php b/Classes/Middleware/McpServerMiddleware.php
index 924d77e..6df27e1 100644
--- a/Classes/Middleware/McpServerMiddleware.php
+++ b/Classes/Middleware/McpServerMiddleware.php
@@ -90,7 +90,7 @@ readonly class McpServerMiddleware implements MiddlewareInterface
             $baseUrl .= ':' . $uri->getPort();
         }

-        $resourceMetadataUrl = $baseUrl . '/.well-known/oauth-protected-resource';
+        $resourceMetadataUrl = $baseUrl . '/.well-known/oauth-protected-resource' . self::MCP_PATH;

         $body = $this->streamFactory->createStream(json_encode(['error' => $error], JSON_THROW_ON_ERROR));

diff --git a/Classes/Middleware/OAuthMiddleware.php b/Classes/Middleware/OAuthMiddleware.php
index dde6a74..2463e01 100644
--- a/Classes/Middleware/OAuthMiddleware.php
+++ b/Classes/Middleware/OAuthMiddleware.php
@@ -71,7 +71,7 @@ readonly class OAuthMiddleware implements MiddlewareInterface

         return match (true) {
             $path === self::METADATA_PATH && $method === 'GET' => $this->handleMetadata($request),
-            $path === self::RESOURCE_METADATA_PATH && $method === 'GET' => $this->handleResourceMetadata($request),
+            $this->isResourceMetadataPath($path) && $method === 'GET' => $this->handleResourceMetadata($request),
             $path === self::AUTHORIZE_PATH && $method === 'GET' => $this->handleAuthorizeGet($request),
             $path === self::AUTHORIZE_PATH && $method === 'POST' => $this->handleAuthorizePost($request),
             $path === self::TOKEN_PATH && $method === 'POST' => $this->handleToken($request),
@@ -120,6 +120,12 @@ readonly class OAuthMiddleware implements MiddlewareInterface
         return $this->createJsonResponse(200, $metadata);
     }

+    private function isResourceMetadataPath(string $path): bool
+    {
+        return $path === self::RESOURCE_METADATA_PATH
+            || $path === self::RESOURCE_METADATA_PATH . '/mcp';
+    }
+
     private function handleAuthorizeGet(ServerRequestInterface $request): ResponseInterface
     {
         /** @var array<string, mixed> $params */
